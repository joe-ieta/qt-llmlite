# v0.2.1

This release is centered on validating `qtllm` as a foundation for business-agent development, not on shipping a final translation product.

Highlights:

- added `src/agents/pdf_translator_agent/` as an independent business-agent validation application
- validated skill-oriented workflow composition on top of the shared `qtllm` infrastructure
- validated per-skill model routing and MCP-backed business-agent integration
- added agent-focused documentation, delivery checklist, and versioned design archive

Agent-specific release framing remains in:

- `docs/agents/releases/v0.2.1.md`
