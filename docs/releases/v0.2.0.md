# v0.2.0

## Highlights

- Expanded the `qtllm` developer documentation with a dedicated layered guide under `docs/developer-guide/`, covering architecture, core APIs, tool runtime, MCP integration, observability, scenarios, and end-to-end examples.
- Refreshed top-level project documentation so the repository structure, architecture description, integration guidance, and testing baseline better match the current codebase.
- Continued improving the MCP server manager application, including UI restructuring, log surfacing at the top level, and clearer server-oriented tab labeling.
- Carried forward the `qtllm` branch work that strengthens the core library and its surrounding application integration surface without changing the documented public usage model.

## Notes

- Release tag: `v0.2.0`
- This release includes the work merged after `v0.1.2`.
- The change set combines documentation consolidation, a new developer manual, and MCP manager UX refinements.
